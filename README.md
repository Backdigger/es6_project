```
To set up the project, do:

1. npm install
2. npm install -g gulp
3. npm install --save-dev gulp
4. gulp webserver

Technologies used:

- HTML 5
- CSS 3
- JavaScript (ES-6) + EventEmitter
- npm
- gulp


Task: 
- Create plugin for customizable modal windows using ES-6 Class construction
- Window to have a drag and drop and resize capabilities
- Public methods to get/set position/size/style
- Events to show/close or remove event listeners

```
